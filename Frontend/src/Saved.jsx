import React from 'react';
import { Box, Typography } from '@mui/material';

const Saved = () => {
  return (
    <Box sx={{ padding: 3 }}>
      <Typography variant="h4">Saved</Typography>
      <Typography variant="body1">View saved items here.</Typography>
    </Box>
  );
};

export default Saved;
