import React from 'react';
import { Box, Typography } from '@mui/material';

const History = () => {
  return (
    <Box sx={{ padding: 3 }}>
      <Typography variant="h4">History</Typography>
      <Typography variant="body1">View history here.</Typography>
    </Box>
  );
};

export default History;
