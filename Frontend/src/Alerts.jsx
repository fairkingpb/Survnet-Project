import React from 'react';
import { Box, Typography } from '@mui/material';

const Alerts = () => {
  return (
    <Box sx={{ padding: 3 }}>
      <Typography variant="h4">Alerts</Typography>
      <Typography variant="body1">View alerts here.</Typography>
    </Box>
  );
};

export default Alerts;
